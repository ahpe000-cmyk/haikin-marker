# 決定事項と閾値の記録

> CLAUDE.md 掟2・§6 にもとづく記録。新しいものを上に足す。

## 2026-09-05 着手条件（BEFoAF完成待ち）の解除
- 花園より本セッションで「BEFoAFの完成は待たずに進めてください」と指示があった
- CLAUDE.md の着手条件（優先0の完成後）は雇用主本人の指示により解除。P1開発に着手
- CLAUDE.md v1.0 の記載自体は歴史的経緯として残す

## 2026-09-05 配置場所: `atama-taiso/` サブディレクトリ
- このリポジトリ（haikin-marker）にはARCダッシュボード（GitHub Pagesデプロイ、
  ルートの package.json / .github/workflows/deploy.yml を使用）が既に存在する
- CLAUDE.md §11 はルート直下の構成を想定しているが、ルート配置にすると
  main マージ時に ARC の `npm ci && npm run build` が壊れるため、
  アプリ一式を `atama-taiso/` に隔離した（§11 の構成はその中でそのまま維持）
- Vercel 側はプロジェクト設定の Root Directory を `atama-taiso` にする必要がある
- **推奨**: 頭体操アプリは専用リポジトリへ分離する（花園の判断待ちではなく提案）

## 2026-09-05 P1の「ごはんを記録」は手入力版で先行
- ホーム3ボタンの1つ「ごはんを記録」が P1 で行き先なしになるのを避けるため、
  `/meals/new` を手入力（source = 'manual'）で実装した
- カメラ撮影 + AI判定（source = 'photo_ai'）は予定どおり PR #8（P2）で同じ画面に足す

## 2026-09-05 記憶クイズの生成ロジックを先行実装
- ルーティン仕様 §3[3]（食事データ7日分以上の時だけ出す）の判定が P1 の
  セッション画面に必要なため、`src/lib/quiz/memory.ts` を仕様どおり実装した
- 実データが7日分たまるまで画面には出ない（P2 の受け入れ確認は PR #9 時点で行う）

## 2026-09-05 音声入力はスマホキーボードのマイクを使う
- 専用の録音UIは作らない（増やさない原則）。入力欄の下に
  「キーボードのマイクのボタンで、声でも入力できます」の一言を置いた

## 2026-09-05 ホーム画面の補助リンク
- §10 はホームを3ボタンのみとするが、/history と /settings への入口が
  仕様上どこにもないため、ホーム下部に小さな文字リンク2つ
  （「これまでの記録」「せってい」）を置いた。主要ボタンは3つのまま

## 2026-09-05 日記の見守り共有は「RLSで一切読めない」で保留
- 14-2（日記本文をどこまで見せるか）が判断待ちのため、
  watcher 向けの diary_entries SELECT ポリシー自体を作っていない
- 決定後、PR #10 でポリシーと画面を足す

## （予約）食事写真AI判定の confidence 閾値
- 仮に 0.6（CLAUDE.md §6）。PR #8 実装時にここへ実測値と調整を記録する
